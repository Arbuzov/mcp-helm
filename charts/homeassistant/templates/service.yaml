apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-homeassistant-helm.fullname" . }}
  labels:
    {{- include "mcp-homeassistant-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-homeassistant-helm.selectorLabels" . | nindent 4 }}
