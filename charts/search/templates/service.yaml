apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-helm-chart.fullname" . }}
  labels:
    app: {{ include "search-helm-chart.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "search-helm-chart.name" . }}